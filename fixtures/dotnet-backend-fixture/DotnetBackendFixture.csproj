<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>DotnetBackendFixture</RootNamespace>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <!-- Relative on purpose: the hermetic gate copies sdk/ and fixtures/ together to a
         different absolute path (the PORTABILITY bar), and this reference must still land. -->
    <ProjectReference
      Include="../../sdk/reproit-backend-dotnet/ReproitBackend/ReproitBackend.csproj" />
  </ItemGroup>

</Project>
