{"applicationPin": "3.38.10 from LocalSend .fvmrc", "check": "worker-image-flutter-version-versus-application-pin", "command": "flutter pub get in app/ at 3ec2d77875fc31dab21548ae4966ca693e8b2733", "conclusion": "the lane image's Flutter was raised to 3.41.6 after the groundwork that recorded these revisions as buildable at 3.35.6, and the newer SDK's bundled test packages make the application's own lockfile unsolvable. The build has to use the version the application pins, which is checked out inside the build container rather than by changing the image, so the lane keeps one image for every target", "imageFlutter": "3.41.6", "resultAtApplicationPin": "Got dependencies", "resultAtImageVersion": "version solving failed: every version of flutter_test from sdk depends on both matcher 0.12.19 and test_api 0.7.10, while localsend_app depends on mockito 5.5.0 and test ^1.26.2, so mockito 5.5.0 is incompatible with test >=1.25.13", "worker": "linux/amd64 lane image reproit-android-x86-07cae9bc60e8a40263ee"}
{"affectedRevision": "3ec2d77875fc31dab21548ae4966ca693e8b2733", "candidate": "localsend-receive-link-2975", "check": "offline-trigger-and-observable-derived-from-source", "diff": "receive_page.dart: isLink gains a whitespace guard, so a message that merely starts with a URL is no longer classified as a link", "fixedRevision": "9e4a5985b5fd1377f7c4c1fa9127a00b8fc9abff", "issue": "https://github.com/localsend/localsend/issues/2904", "neighboringLegalBehavior": "a bare URL with no whitespace stays a link on both revisions, so the link path itself is untouched and only the whitespace case moves", "observable": "the receive page subtitle, which is t.receivePage.subTitleLink 'sent you a link:' when isLink is true and subTitleMessage 'sent you a message:' when it is false, plus the open-link button that only exists under if (vm.isLink). Both are strings a semantics or widget dump can show, which is what the profile channel allows", "repository": "https://github.com/localsend/localsend", "status": "derived from source and not yet executed: no run was performed, so this pair is not yet verified to discriminate", "trigger": "POST /api/localsend/v2/prepare-upload on loopback with exactly one file of fileType text carrying the message in preview. ReceiveSessionState.message returns that preview only for a single text file, which is what puts the receive page into the state under test, and it needs no second device and no network"}
