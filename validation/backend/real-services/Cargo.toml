[package]
name = "reproit-real-backend-validator"
version = "0.1.0"
edition = "2021"
publish = false

[workspace]

[dependencies]
reproit = { path = "../../../crates/reproit", default-features = false }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
