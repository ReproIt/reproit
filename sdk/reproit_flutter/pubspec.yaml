name: reproit_flutter
description: ReproIt production telemetry for Flutter. Capture structural UI paths so production bugs replay as deterministic tests.
version: 0.1.0
homepage: https://reproit.com
repository: https://github.com/ReproIt/reproit/tree/main/sdk/reproit_flutter
issue_tracker: https://github.com/ReproIt/reproit/issues
topics:
  - testing
  - observability
  - crash-reporting
  - flutter
environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"
dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  crypto: ^3.0.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
flutter:
